import styled from 'styled-components'

export const HomeContainer = styled.div`
    display: flex;
    flex-direction: column;
    max-width: calc(100% - 230px); // 100% of the screen minus the Sidebar size
`